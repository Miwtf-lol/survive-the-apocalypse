local RunService = game:GetService("RunService")

local Players = game:GetService("Players")

local player = Players.LocalPlayer



-- UI Library

local UILibrary = loadstring(game:HttpGet("https://raw.githubusercontent.com/zxcursedsocute/UI-Library/refs/heads/main/UI-Library.txt"))()

local windows = UILibrary.CreateWindow("MIYU HUB ","","590","V1.1")

local PlayerTab = windows:AddTab("PLAYER","PLAYER")

PlayerTab:AddSection('MAIN')



-- Cấu hình

local COLLECT_RANGE = 25 

local FOLDER_NAME = "DroppedItems"

local collector = false 



PlayerTab:AddToggle({

    Name = "Collector Item",

    Description = "Hút đồ bằng lực (Không lỗi Store)",

    Default = false,

    Callback = function(v)

        collector = v

        if not v then

            local droppedItems = workspace:FindFirstChild(FOLDER_NAME)

            if droppedItems then

                for _, item in pairs(droppedItems:GetChildren()) do

                    local p = item:IsA("BasePart") and item or item:FindFirstChildWhichIsA("BasePart")

                    if p then

                        -- Xóa lực hút khi tắt

                        local force = p:FindFirstChild("MiyuForce")

                        if force then force:Destroy() end

                        p.CanCollide = true

                        p.Anchored = false

                    end

                end

            end

        end

    end,

})



RunService.RenderStepped:Connect(function()

    if not collector then return end

    

    local char = player.Character

    local hrp = char and char:FindFirstChild("HumanoidRootPart")

    if not hrp then return end



    local droppedItems = workspace:FindFirstChild(FOLDER_NAME)

    if not droppedItems then return end



    for _, item in pairs(droppedItems:GetChildren()) do

        local p = item:IsA("BasePart") and item or item:FindFirstChildWhichIsA("BasePart")

        if p then

            local distance = (hrp.Position - p.Position).Magnitude

            if distance <= COLLECT_RANGE then

                p.CanCollide = false

                p.Anchored = false

                

                -- TẠO LỰC HÚT VẬT LÝ (Thay vì dùng CFrame)

                local force = p:FindFirstChild("MiyuForce") or Instance.new("BodyVelocity")

                force.Name = "MiyuForce"

                force.Parent = p

                

                local targetPos = (hrp.CFrame * CFrame.new(0, 0, 5)).Position

                local direction = (targetPos - p.Position)

                

                force.MaxForce = Vector3.new(90000, 90000, 90000) -- Đủ mạnh để hút nhưng không làm đồ bị "cứng"

                force.Velocity = direction * 8 -- Tốc độ bay theo sau lưng

            else

                -- Nếu ngoài tầm hút thì xóa lực để nó rớt xuống

                local force = p:FindFirstChild("MiyuForce")

                if force then force:Destroy() end

                p.CanCollide = true

            end

        end

    end

end)
